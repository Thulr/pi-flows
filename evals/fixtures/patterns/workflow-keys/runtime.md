# Runtime facts

There are three production regions: us-east, us-west, and eu-central. Metrics are per-region. The signing service can restore old-key signing in under four minutes. Before revocation, operators must verify NTP clock skew is below ten seconds in every region.
