# Volume pressure response

Pause the producing snapshot job. Delete only orphan snapshots older than seven days; never remove attached snapshots or the volume. Verify usage remains below 80% for two checks before resuming the job.
